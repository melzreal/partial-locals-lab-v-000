<%= render 'classroom' %>

We now want to call all students in the class:

<%= @students.each do |student|

  <%= student.name %>

 <%= student.birthday.strftime("%m/%d/%Y")  %>

 <%= student.hometown.capitalize %>



<% end %>
