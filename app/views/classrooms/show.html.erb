<%= render partial: "classroom", locals: {classroom: @classroom} %>

We now want to call all students in the class:
<% @classroom.students.each do |st| %>
<%= render partial: "students/student", locals: {student: st} %>
<% end %>
